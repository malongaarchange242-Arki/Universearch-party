/**
 * DEPRECATED: PostgreSQL Pool
 * This file is kept for backward compatibility but is no longer needed.
 * All database operations now use Supabase SDK from config/supabase.ts
 */

// Placeholder export for compiled code
export default null;
